class Circle
{
     constructor()
    {
        this.x = x;
        this.y = y;
        this.radius = radius;
    }
    //method/function that does stuff
    display()
    {
        ellipse(this.x,this.y,this.radius);
    }
}